export const ButtonDark = ({ children }) => {
  return <button className="button-dark">{children}</button>;
};

export const Button = ({ children }) => {
  return <button className="button-normal">{children}</button>;
};
